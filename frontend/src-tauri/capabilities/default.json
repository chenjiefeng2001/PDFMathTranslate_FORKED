{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口能力：核心默认 + 原生保存/选夹对话框（下载到指定位置）。",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
