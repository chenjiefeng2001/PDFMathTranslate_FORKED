# 7I-6B — Wiring F10 / F9 + F7 Contract Freeze

**Status:** ✅ COMPLETE — followed the frozen order **F10 → F9 → F7 contract**;
F5 untouched.  No new detector semantics, no `pdf2zh/` production change, no
renderer/provenance/emitter/CID/clip changes.  Pure wiring of evidence that
already existed in-pipeline.

## What changed

`dual_forensics/defect.py`:
- Added **page-level detectors** `_detect_f10_object_lost_page` and
  `_detect_f9_text_visual_mismatch_page`; `_PAGE_DETECTORS` now dispatches
  `(traces, dual_page)` so the page-level render evidence reaches coverage.
- `run_defect_detectors` / `coverage_page` now run F9/F10 off `dual_page`.
- Contract enforced in-code:
  - **F10**: `present` (with provenance) → PASS; `dangling`/`stray` → FAIL@render;
    no provenance → **SKIP**; block never owed by plan → **SKIP** (not PASS).
  - **F9**: `content_stream.checked` + no anomaly → PASS; anomaly → FAIL@render;
    no content stream → **SKIP**.
- Boundary preserved: F10 ≠ F8 (text clip of a *present* block), ≠ F5 (float
  placement).  F10 is strictly object presence.

Callers now thread page evidence:
- `__main__.py`, `doc/7i4/residual_corpus_scan.py`, `doc/7i4/full_corpus_baseline.py`
  surface the ID-direct provenance summary as `dual_page["id_direct"]` and the
  content-stream verdict as `dual_page["content_stream"]`.

## Coverage transition (5 books / 33 pages)

| Defect | Before (7I-6A) | 7I-6B | F1–F10 residual |
|---:|---:|---:|---:|
| F5 | SKIP | **SKIP** (0/33 evaluated; representation gap) | 0 |
| F7 | NOT_MEASURED | **NOT_MEASURED** (contract frozen, gated) | 0 |
| F9 | NOT_MEASURED | **PASS 33/33** | 0 |
| F10 | NOT_MEASURED | **PASS 33/33** | 0 |

- **F9** measured via `content_stream_anomaly`: emitter clean since 7H-2B → PASS.
- **F10** measured via ID-direct provenance: all 489 blocks present, 0 dangling /
  0 stray → PASS.
- **F4** stays **1 @ p300 @ parser** — the only real residual; **no manufactured
  defect** from the wiring (defect histograms otherwise all 0).

## Acceptance confirmation

> Existing 33-page corpus: F10 went NOT_MEASURED → PASS and F9 → PASS, and the
> wiring did **not** create any new defect.  Residual is unchanged at **1 (F4)**.

## F7 contract (frozen, not run — identity corpus is a null test)

```
F7 = source text leftover / duplication
  PASS:          real-translation evidence + rendered lacks untranslated residue
  FAIL:          real-translation evidence + residue/duplication positively identified
  SKIP:          translation evidence insufficient / identity translation
  NOT_MEASURED:  detector contract not wired  (current state)
INVARIANT: identity (translated_text == source_text) must NEVER be treated as F7 FAIL.
```

Locked by `tests/test_evidence_wiring_7i6b.py::test_f7_identity_translation_is_not_fail`
and the coverage contract.  No F7 detector is run against the identity corpus.

## Tests & verification

- New `tests/test_evidence_wiring_7i6b.py` — 10 F9/F10/F7/F5 contract tests
  (present PASS, dangling/stray FAIL, no-evidence SKIP, identity-is-not-F7,
  independence: wiring doesn't cascade to F1/F3).
- Updated `test_evidence_inventory_7i6a.py` + `test_detector_coverage_7i4.py` to
  reflect F9/F10 = SKIP-without-evidence (post-wiring), F7 = NOT_MEASURED.
- Full top-level suite **2371 passed, 3 skipped** (`--ignore=tests/v3`); forensic
  subset 136 pass.  black/flake8 clean.

## Artifacts (regenerated by the wiring, evidence-backed)

- `doc/7i4-corpus-scan/{summary.json, report.md}` — per-book coverage table
  (F9/F10 now PASS).
- `doc/7i4-corpus-baseline/{summary.json, report.md}` — global coverage matrix
  (F9=31 PASS, F10=31 PASS, F7=31 NOT_MEASURED, F5=31 SKIP; total residual = 1).

## Next
7I-6 series now has every F-id at an honest state.  F5 remains a representation
gap (no model change without real float evidence); F7 is contract-frozen pending
a real-translation corpus.  No fabrication, no forced PASS.